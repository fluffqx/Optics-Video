The remarkable thing is that the shape of f or g can be completely arbitrary — a Gaussian pulse, a square wave, a single crest, anything at all. Whatever shape the wave has at time zero, it will retain exactly that shape as it propagates. This shape-preserving property follows directly from the structure of the wave equation, and it only holds when v is the same for all frequencies — which we call a non-dispersive medium.